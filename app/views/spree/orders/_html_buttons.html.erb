<% buttons = Spree::HtmlInvoice::Config[:print_buttons] || "invoice"
   buttons = buttons.split(",").collect{|b| b.strip } %>
<%  buttons.each do |button| %>
<%= link_to(t(button.to_s+".print"), "/orders/#{@order.number}/#{button}" , :target => "_blank") %>
<%end%>
